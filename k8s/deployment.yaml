kind: Deployment
apiVersion: apps/v1
metadata:
  name: mydeployment
  labels:
    app: petshop
spec:
  replicas: 4
  selector:
    matchLabels:
      app: petshop
  template:
    metadata:
      labels:
        app:  petshop
    spec:
      containers:
        - name: mycontainer1
          image: skumarjira/petimage
          ports:
            - containerPort: 8080


